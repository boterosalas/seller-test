import { ItemConversationDirective } from './item-conversation.directive';

describe('ItemConversationDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemConversationDirective();
    expect(directive).toBeTruthy();
  });
});
