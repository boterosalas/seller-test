import { DateNoGtmPipe } from './date-no-gtm.pipe';

describe('DateNoGtmPipe', () => {
  it('create an instance', () => {
    const pipe = new DateNoGtmPipe();
    expect(pipe).toBeTruthy();
  });
});
