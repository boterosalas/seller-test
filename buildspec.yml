version: 0.2

phases:
  build:
    commands:
      - npm install
      - npm install -g @angular/cli
      - ng build --prod
artifacts:
  files:
    - deploy/dist/**/*
    - packaging = "NONE"
    